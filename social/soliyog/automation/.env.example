# Copy to .env (which is git-ignored). Do not commit real values.

# Cloudflare Workers AI — for gen-image.mjs
# Account ID: dash.cloudflare.com -> any page, right sidebar "Account ID"
# API token:  dash.cloudflare.com -> My Profile -> API Tokens -> Create Token
#             -> template "Workers AI (Read + Run)" -> Continue -> Create
CF_ACCOUNT_ID=
CF_API_TOKEN=

# Meta Graph API — for the daily FB+IG poster (Part 5, not built yet)
META_TOKEN=
FB_PAGE_ID=
IG_USER_ID=
